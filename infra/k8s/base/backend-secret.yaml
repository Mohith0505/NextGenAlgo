apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: nextgen-algo
  labels:
    app: backend
  annotations:
    description: Broker credential placeholders; populate via secure secret manager
stringData:
  SECRET_KEY: change-me
  BROKER_API_KEY: placeholder
  BROKER_API_SECRET: placeholder
  POSTGRES_PASSWORD: postgres
